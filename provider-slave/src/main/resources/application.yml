server:
  port: 9001

spring:
  application:
    name: provider
  cloud:
     nacos:
       discovery:
         server-addr: 192.168.0.39:8848

#暴露监控
management:
  endpoints:
    web:
      exposure:
        include: '*'
